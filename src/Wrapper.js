const Wrapper = ({ children }) => {
    // const { children } = props;
    return (
        <section className="Wrapper">
            {children}
        </section>
    )
}

export default Wrapper;